<h1>Approving or Rejecting a Pet for Adoption</h1>

<% @pets.each do |pet| %>
  <p> <%=link_to "Pet Name: #{pet.name}", "/pets/#{pet.id}" %> <%= button_to "Approve #{pet.name}", "/admin/applications/#{@application.id}", method: :get, params: {approve: pet} %> </p>
<% end %>
